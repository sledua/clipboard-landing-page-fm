// Create an arrow function to add list items to the array
const addListItem = (item) => {
    listItems.push(item);
};

// Call the arrow function to add list items
addListItem("Item 4");
addListItem("Item 5");
addListItem("Item 6");

// Print the updated list items
console.log(listItems);
